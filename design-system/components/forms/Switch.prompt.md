The scenario lever. Every toggle in the control rail is one of these.

```jsx
<Switch label="Fragrance-free reformulation" checked={fragranceFree} onChange={setFragranceFree} />
```

On state is teal — deliberately the "would buy" colour, since levers exist to move the panel upward. Rows in a stack are separated by a 1px hairline, first row excepted.
