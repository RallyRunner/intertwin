Text input, also the chat composer.

```jsx
<Input placeholder="Ask the whole panel, or @ a persona by name…" />
<Input prefix="$" suffix="USD" type="number" value={38} />
<Input as="textarea" placeholder="Claim wording, verbatim" />
```
