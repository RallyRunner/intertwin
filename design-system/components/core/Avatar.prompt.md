Persona identity mark — initials on a persona colour.

```jsx
<Avatar name="Marisol" color="var(--persona-1)" size={38} />
```

A persona's colour is assigned once and never changes: it is how a user tracks the same individual from the panel grid into the chat thread. 38px on cards, 26px in chat, 56px+ on persona detail.
