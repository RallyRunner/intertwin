Uppercase mono section label — the only place tracking-caps is used in the app.

```jsx
<SectionTitle icon={<LiveDot/>} action={<Button variant="ghost" size="sm">Expand all</Button>}>
  Persona reactions
</SectionTitle>
```
