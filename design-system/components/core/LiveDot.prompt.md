Pulsing teal dot. Marks the region of the UI that re-evaluates when a lever moves.

```jsx
<SectionTitle icon={<LiveDot />}>Aggregate purchase intent — 6 personas</SectionTitle>
```

Use at most one per screen; if everything pulses, nothing reads as live.
