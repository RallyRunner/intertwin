Icon-only square button for toolbar and card affordances.

```jsx
<IconButton name="refresh-cw" label="Re-run panel" />
<IconButton name="x" label="Close" variant="ghost" size="sm" />
```

Requires the Lucide UMD script on the page (see Icon). Border turns iris on hover.
