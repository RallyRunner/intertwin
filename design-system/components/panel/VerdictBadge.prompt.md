The three-state purchase-intent badge — the single most repeated element in the product.

```jsx
<VerdictBadge verdict="buy" />
<VerdictBadge verdict="pass" moved />
```

`buy | fence | pass` → teal / gold / coral, always. The `moved` flag adds the iris "changed in this re-run" marker; clear it on the next interaction so it stays meaningful.
